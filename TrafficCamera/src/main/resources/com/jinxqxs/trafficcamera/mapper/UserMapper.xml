<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinxqxs.trafficcamera.mapper.UserMapper">

    <select id="selectByUsername" resultType="com.jinxqxs.trafficcamera.pojo.User">
        SELECT id, username, password, role, create_time as createTime
        FROM sys_user WHERE username = #{username}
    </select>

    <select id="selectById" resultType="com.jinxqxs.trafficcamera.pojo.User">
        SELECT id, username, password, role, create_time as createTime
        FROM sys_user WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.jinxqxs.trafficcamera.pojo.User">
        SELECT id, username, role, create_time as createTime FROM sys_user
    </select>

    <insert id="insert">
        INSERT INTO sys_user (username, password, role, create_time)
        VALUES (#{username}, #{password}, #{role}, #{createTime})
    </insert>

    <update id="update">
        UPDATE sys_user
        <set>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="role != null and role != ''">role = #{role},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>

</mapper>
